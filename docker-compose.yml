services:
  copilot-proxy:
    build:
      context: .
      network: host
    ports:
      - "7677:7677"
    volumes:
      - copilot-proxy-data:/root/.config/copilot-proxy
    restart: always

  open-webui:
    image: ghcr.io/open-webui/open-webui:main
    extra_hosts:
      - "host.docker.internal:host-gateway"
    ports:
      - "7676:8080"
    environment:
      - OPENAI_API_BASE_URL=http://copilot-proxy:7677/v1
      - OPENAI_API_KEY=sk-dummy
      - ENABLE_PERSISTENT_CONFIG=false
      - >-
        TOOL_SERVER_CONNECTIONS=[
        {"url":"http://host.docker.internal:7678/chrome-devtools","path":"openapi.json","auth_type":"bearer","key":"","config":{"enable":true}},
        {"url":"http://host.docker.internal:7678/filesystem","path":"openapi.json","auth_type":"bearer","key":"","config":{"enable":true}},
        {"url":"http://host.docker.internal:7678/sequential-thinking","path":"openapi.json","auth_type":"bearer","key":"","config":{"enable":true}},
        {"url":"http://host.docker.internal:7678/fetch","path":"openapi.json","auth_type":"bearer","key":"","config":{"enable":true}},
        {"url":"http://host.docker.internal:7678/time","path":"openapi.json","auth_type":"bearer","key":"","config":{"enable":true}}
        ]
    volumes:
      - open-webui:/app/backend/data
    depends_on:
      - copilot-proxy
    restart: always

volumes:
  copilot-proxy-data:
  open-webui:
